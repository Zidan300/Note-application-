import { useState } from 'react'; import { useAuth } from '../../../contex/authContext.jsx';
export default function Signup({ onNavigate }) { const { signup } = useAuth(); const [form, setForm] = useState({ username: '', email: '', password: '' }); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const submit = async (e) => { e.preventDefault(); setError(''); setBusy(true); try { await signup(form); onNavigate('/dashboard'); } catch (err) { setError(err.response?.data?.message || err.message || 'Signup failed'); } finally { setBusy(false); } }; return <main className="auth-page"><form className="auth-card" onSubmit={submit}><h1>Create your account</h1><p>Keep your stays and notes organized securely.</p>{error && <div className="auth-error" role="alert">{error}</div>}<input required minLength="3" placeholder="Username" value={form.username} onChange={(e) => setForm({ ...form, username: e.target.value })} /><input required type="email" placeholder="Email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /><input required minLength="8" type="password" placeholder="Password (8+ characters)" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} /><button disabled={busy} type="submit">{busy ? 'Creating…' : 'Sign up'}</button><div className="auth-footer">Already registered? <button type="button" onClick={() => onNavigate('/login')}>Log in</button></div></form></main>; }

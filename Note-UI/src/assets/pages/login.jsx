import { useState } from 'react'; import { useAuth } from '../../../contex/authContext.jsx';
export default function Login({ onNavigate }) { const { login } = useAuth(); const [form, setForm] = useState({ email: '', password: '' }); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const submit = async (e) => { e.preventDefault(); setError(''); setBusy(true); try { await login(form); onNavigate('/dashboard'); } catch (err) { setError(err.response?.data?.message || err.message || 'Login failed'); } finally { setBusy(false); } }; return <AuthPage title="Welcome back" subtitle="Sign in to your SecureStay workspace" error={error} onSubmit={submit} fields={<><input required type="email" placeholder="Email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /><input required type="password" placeholder="Password" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} /></>} busy={busy} action="Log in" footer={<>New here? <button type="button" onClick={() => onNavigate('/signup')}>Create an account</button></>} />; }
function AuthPage({ title, subtitle, error, onSubmit, fields, busy, action, footer }) { return <main className="auth-page"><form className="auth-card" onSubmit={onSubmit}><h1>{title}</h1><p>{subtitle}</p>{error && <div className="auth-error" role="alert">{error}</div>}{fields}<button disabled={busy} type="submit">{busy ? 'Please wait…' : action}</button><div className="auth-footer">{footer}</div></form></main>; }
